name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Executar comandos
        run: |
          echo "Executando comandos na VM local!"
          # Adicione os comandos que você deseja executar
